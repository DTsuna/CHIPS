
&controls
      
      !gradT_excess_lambda1 = -1 ! force MLT++

      use_other_alpha_mlt = .true.
      x_ctrl(21) = 3.0 ! alpha_H
      x_ctrl(22) = 1.5 ! alpha_other
      x_ctrl(23) = 0.5 ! H_limit 
         ! use alpha_H if cell X >= H_limit; else use alpha_other


/ ! end of controls namelist

